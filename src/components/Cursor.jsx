import styled from 'styled-components/macro';

const StyledCursor = styled.span`
  display: inline-block;
  width: 14px;
  height: 20px;
  background-color: #fff;
  color: #fff;
`;

const Cursor = () => <StyledCursor />;

export default Cursor;
